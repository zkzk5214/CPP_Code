#pragma once 
/*
    #pragma once can prevent the code of the entire from being repeatedly
        included(not supported by older compilers, earlier than GCC 3.4)
    #pragma once can only act to the entire header file.
*/ 
void test();
